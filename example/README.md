# An Example WMAP Message
This directory contains two files:

* [message.json](message.json) which shows a minimal WMAP message
* [message.json.sig](message.json.sig) which shows a valid signature for
  that message.
